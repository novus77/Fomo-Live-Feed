# v0.5.0 Website Release Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Verify and publish the existing v0.5.0 website content to the production Vercel domain.

**Architecture:** Treat `package.json` and `CHANGELOG.md` as release truth, while `website/index.html` and `website/main.js` remain the static presentation and download entry point. Since release commit `dcebccf` already updated the website, verify rather than rewrite matching content, then deploy the existing `website` directory.

**Tech Stack:** Static HTML/CSS/JavaScript, Node.js test runner, GitHub CLI, Vercel CLI

---

### Task 1: Verify release-content consistency

**Files:**
- Read: `package.json`
- Read: `CHANGELOG.md`
- Verify: `website/index.html`
- Verify: `website/main.js`
- Test: `website/homepage.test.mjs`

- [ ] Run `node --test website/homepage.test.mjs` and require zero failures.
- [ ] Run `node --check website/main.js` and require exit code zero.
- [ ] Check CSS brace balance and run `git diff --check`.
- [ ] Confirm `v0.5.0`, Fomo/Pump copy, five release entries, and the versioned Chrome ZIP URL are present.

### Task 2: Verify the GitHub release asset

**Files:**
- Read-only remote verification of GitHub Release `v0.5.0`

- [ ] Run `gh release view v0.5.0 --repo novus77/Fomo-Live-Feed --json tagName,isDraft,isPrerelease,assets`.
- [ ] Require a published non-prerelease tag and `Fomo-Live-Feed-v0.5.0-chrome.zip` asset.

### Task 3: Deploy production website

**Files:**
- Deploy: `website/`

- [ ] Run `npx --yes vercel@latest deploy website --prod --yes --scope jobmake77s-projects`.
- [ ] Require Vercel `readyState` to be `READY`, target to be `production`, and alias output to include `https://fomolivefeed.online`.
- [ ] Report the production URL and custom domain without fetching the deployed page.

### Task 4: Make v0.5.0 capabilities visible on the homepage

**Files:**
- Modify: `website/homepage.test.mjs`
- Modify: `website/index.html`
- Modify: `website/styles.css`

- [ ] Add region-scoped failing assertions for the hero headline, default Fomo/Pump feed, source and amount filters, and dual-source feature card.
- [ ] Run `node --test website/homepage.test.mjs` and confirm the assertions fail for missing visible content.
- [ ] Update the two-line hero headline to explicitly name Fomo and Pump.
- [ ] Add source status, source badges, and one event from each source to the default feed demo.
- [ ] Add exclusive source controls and a buy-amount range preview to the filter demo.
- [ ] Update the primary feature card to explain chronological mixing, identity deduplication, and amount filtering.
- [ ] Run homepage tests, JavaScript syntax validation, CSS brace validation, and `git diff --check`.
- [ ] Redeploy from the repository root and require Vercel `READY` plus the custom-domain alias.
