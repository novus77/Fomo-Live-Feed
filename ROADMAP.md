# Roadmap

本文档记录尚未发布、后续版本必须完成的产品方向。完成开发并正式发布后，应将对应内容
从本文档移入 `CHANGELOG.md`。

## 当前状态

此前列出的买入声音提示与代币名称跳转已经在 `v0.3.0` 完成，并移入
`CHANGELOG.md`。

## 下一版本已确认

### 1. 用户名内联备注

- 用户名后常驻 `＋备注`，已有备注显示为紧凑标签。
- 在当前卡片内联编辑，支持 Enter 保存、Esc 取消和空值清除。
- 复用现有本地交易员标注，以稳定 `traderId` 保存，并同步更新同一用户的全部卡片。
- 备注继续参与搜索，长内容截断，不增加卡片高度。

### 2. 金额显示设置

- 买入金额、卖出金额和 MC 市值分别保存字号与颜色，三组互不影响。
- 每组提供四档预设、11–18 px 自定义滑杆、主题预设色和自定义颜色选择器。
- 支持单组恢复默认和全部恢复默认，并在设置中实时预览。
- 使用结构化持久设置与版本迁移，保持单行交易摘要和当前信息密度。
